// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
//export default defineConfig({});

export default {
    server: {
      port: 4321, // Asegúrate de que este puerto sea el correcto
    },
  };
